// Generated by CoffeeScript 1.6.3
$(function() {
  var $anchors, $wraps, getId;
  $wraps = $('.wrap');
  $anchors = $('.prev-next>div>a');
  $wraps.hide();
  $wraps.first().show();
  getId = function(direction) {
    var currId;
    if (direction == null) {
      direction = "next";
    }
    currId = Number($wraps.not(':hidden').attr('id'));
    if (direction === 'next') {
      if (currId < $wraps.length) {
        return currId + 1;
      } else {
        return 1;
      }
    } else {
      if (currId === 1) {
        return $wraps.length;
      } else {
        return currId - 1;
      }
    }
  };
  $anchors.first().click(function(evt) {
    var nextWrap;
    nextWrap = $("#" + (getId()));
    $wraps.hide();
    return nextWrap.show();
  });
  $anchors.last().click(function(evt) {
    var nextWrap;
    nextWrap = $("#" + (getId('prev')));
    $wraps.hide();
    return nextWrap.show();
  });
  $('section.content').on({
    mouseenter: function() {
      this.contentEditable = true;
      return this.classList.add('focused');
    },
    mouseleave: function() {
      this.contentEditable = false;
      return this.classList.remove('focused');
    }
  }, '.bank-info > tbody > tr:nth-child(2) > td');
  return $('.prev-next').on({
    mouseenter: function() {
      return $anchors.css('display', 'block');
    },
    mouseleave: function() {
      return $anchors.hide();
    }
  });
});
