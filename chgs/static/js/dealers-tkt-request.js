// Generated by CoffeeScript 1.6.3
$(function() {
  var $currTrs, $nextArrow, $remTrs, $table, MAX_ALLOWED_PER_PAGE, getTrs, _ref;
  $table = $('table');
  $nextArrow = $('.prev-next >div> a');
  MAX_ALLOWED_PER_PAGE = 5;
  getTrs = function(trs) {
    var $currTrs, $remTrs;
    if (trs.length > MAX_ALLOWED_PER_PAGE) {
      $currTrs = trs.slice(0, MAX_ALLOWED_PER_PAGE).show();
      $remTrs = trs.slice(MAX_ALLOWED_PER_PAGE).hide();
      return {
        $currTrs: $currTrs,
        $remTrs: $remTrs
      };
    }
    return {
      $currTrs: $(trs),
      $remTrs: null
    };
  };
  _ref = getTrs($('tbody>tr')), $currTrs = _ref.$currTrs, $remTrs = _ref.$remTrs;
  $nextArrow.click(function() {
    var _ref1;
    $currTrs.hide();
    if ($remTrs !== null) {
      _ref1 = getTrs($remTrs), $currTrs = _ref1.$currTrs, $remTrs = _ref1.$remTrs;
      $currTrs.children('td:first-child').text(function(index) {
        return index + 1;
      });
    }
    return $currTrs.show();
  });
  return $('.prev-next').on({
    mouseenter: function(evt) {
      return $nextArrow.show();
    },
    mouseleave: function(evt) {
      return $nextArrow.hide();
    }
  });
});
